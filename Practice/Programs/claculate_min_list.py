

def calculate_mean(numbers):
    # Calculate the sum of all numbers in the list
    total = sum(numbers)
    mean = total / len(numbers)
    return mean

numbers = [5, 10, 3, 8, 2, 15]

mean = calculate_mean(numbers)
print("Mean of sum of numbers:", mean)
